# Advisory baseline for `node build/validate-schemas.js --warn`.
# Format: <repo-relative-path><TAB><exact advisory message>. Use `--no-baseline` to see the full backlog.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/curricula/{id} — operationId "getAcademyCurriculaByID" uses "ID" suffix instead of "Id". Use: "getAcademyCurriculaById". The "Id" casing is universal across all identifiers. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	PUT /api/academy/curricula/{id} — operationId "updateAcademyCurriculaByID" uses "ID" suffix instead of "Id". Use: "updateAcademyCurriculaById". The "Id" casing is universal across all identifiers. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	DELETE /api/academy/curricula/{id} — operationId "deleteAcademyCurriculaByID" uses "ID" suffix instead of "Id". Use: "deleteAcademyCurriculaById". The "Id" casing is universal across all identifiers. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/registrations/test-sessions/start — operationId "startTestByID" uses "ID" suffix instead of "Id". Use: "startTestById". The "Id" casing is universal across all identifiers. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "RegisterToAcademyContentRequest" — property "content_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "contentId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "RegisterToAcademyContentRequest" — property "content_type" uses snake_case (only DB-mirrored fields may use underscores). Use: "contentType". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "AcademyCurricula" — property "badge_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "badgeId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "AcademyCurricula" — property "invite_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "inviteId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "SingleAcademyCurriculaResponse" — property "registration_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "registrationCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "SingleAcademyCurriculaResponse" — property "Invitation" starts with uppercase (must be camelCase, not PascalCase). Use: "invitation". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CreateAcademyCurriculaRequest" — property "badge_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "badgeId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CreateAcademyCurriculaRequest" — property "access_expires_at" uses snake_case (only DB-mirrored fields may use underscores). Use: "accessExpiresAt". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CreateAcademyCurriculaRequest" — property "access_status" uses snake_case (only DB-mirrored fields may use underscores). Use: "accessStatus". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "AcademyCurriculaWithMetrics" — property "registration_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "registrationCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CertificateIssuingAuthority" — property "signature_url" uses snake_case (only DB-mirrored fields may use underscores). Use: "signatureUrl". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Certificate" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Certificate" — property "recipient_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "recipientId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Certificate" — property "recipient_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "recipientName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Certificate" — property "issuing_authorities" uses snake_case (only DB-mirrored fields may use underscores). Use: "issuingAuthorities". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Certificate" — property "issued_date" uses snake_case (only DB-mirrored fields may use underscores). Use: "issuedDate". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Certificate" — property "expiration_date" uses snake_case (only DB-mirrored fields may use underscores). Use: "expirationDate". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Certificate" — property "expires_in" uses snake_case (only DB-mirrored fields may use underscores). Use: "expiresIn". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CurriculaMetadata" — property "detailed_description" uses snake_case (only DB-mirrored fields may use underscores). Use: "detailedDescription". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "AcademyRegistration" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "AcademyRegistration" — property "content_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "contentId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CurriculaCurrentItemData" — property "last_opened" uses snake_case (only DB-mirrored fields may use underscores). Use: "lastOpened". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CurriculaCurrentItemData" — property "content_type" uses snake_case (only DB-mirrored fields may use underscores). Use: "contentType". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CurriculaProgressTracker" — property "current_item" uses snake_case (only DB-mirrored fields may use underscores). Use: "currentItem". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CurriculaProgressTracker" — property "time_spent" uses snake_case (only DB-mirrored fields may use underscores). Use: "timeSpent". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CurriculaProgressTracker" — property "completed_items" uses snake_case (only DB-mirrored fields may use underscores). Use: "completedItems". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "ProgressItemCompleted" — property "completed_at" uses snake_case (only DB-mirrored fields may use underscores). Use: "completedAt". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "ProgressItemCompleted" — property "item_data" uses snake_case (only DB-mirrored fields may use underscores). Use: "itemData". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UpdateCurrentItemRequest" — property "content_type" uses snake_case (only DB-mirrored fields may use underscores). Use: "contentType". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UpdateCurrentItemRequest" — property "item_data" uses snake_case (only DB-mirrored fields may use underscores). Use: "itemData". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "file_path" uses snake_case (only DB-mirrored fields may use underscores). Use: "filePath". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "pass_percentage" uses snake_case (only DB-mirrored fields may use underscores). Use: "passPercentage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "time_limit" uses snake_case (only DB-mirrored fields may use underscores). Use: "timeLimit". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "max_attempts" uses snake_case (only DB-mirrored fields may use underscores). Use: "maxAttempts". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "total_questions" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalQuestions". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "total_questions_in_bank" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalQuestionsInBank". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "total_question_sets" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalQuestionSets". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "total_marks" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalMarks". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Quiz" — property "next_page" uses snake_case (only DB-mirrored fields may use underscores). Use: "nextPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Question" — property "multiple_answers" uses snake_case (only DB-mirrored fields may use underscores). Use: "multipleAnswers". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "Question" — property "correct_answer" uses snake_case (only DB-mirrored fields may use underscores). Use: "correctAnswer". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "QuestionOption" — property "is_correct" uses snake_case (only DB-mirrored fields may use underscores). Use: "isCorrect". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "StartTestRequest" — property "test_abs_path" uses snake_case (only DB-mirrored fields may use underscores). Use: "testAbsPath". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "StartTestRequest" — property "registration_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "registrationId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "QuizSubmission" — property "test_session_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "testSessionId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "QuizSubmission" — property "quiz_abs_path" uses snake_case (only DB-mirrored fields may use underscores). Use: "quizAbsPath". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "QuizSubmission" — property "registration_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "registrationId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "SubmittedAnswer" — property "question_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "questionId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "SubmittedAnswer" — property "selected_option_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "selectedOptionId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "SubmittedAnswer" — property "answer_text" uses snake_case (only DB-mirrored fields may use underscores). Use: "answerText". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "TestSubmission" — property "registration_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "registrationId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "TestSubmission" — property "test_abs_path" uses snake_case (only DB-mirrored fields may use underscores). Use: "testAbsPath". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "TestSubmission" — property "submitted_at" uses snake_case (only DB-mirrored fields may use underscores). Use: "submittedAt". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "TestSubmission" — property "submission_data" uses snake_case (only DB-mirrored fields may use underscores). Use: "submissionData". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "TestSubmission" — property "expires_at" uses snake_case (only DB-mirrored fields may use underscores). Use: "expiresAt". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "QuizEvaluationResult" — property "percentage_scored" uses snake_case (only DB-mirrored fields may use underscores). Use: "percentageScored". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "QuizEvaluationResult" — property "total_marks" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalMarks". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "QuizEvaluationResult" — property "pass_percentage" uses snake_case (only DB-mirrored fields may use underscores). Use: "passPercentage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "QuizEvaluationResult" — property "correct_submissions" uses snake_case (only DB-mirrored fields may use underscores). Use: "correctSubmissions". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "QuizEvaluationResult" — property "attempted_at" uses snake_case (only DB-mirrored fields may use underscores). Use: "attemptedAt". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UserRegistration" — property "curricula_title" uses snake_case (only DB-mirrored fields may use underscores). Use: "curriculaTitle". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UserRegistration" — property "curricula_type" uses snake_case (only DB-mirrored fields may use underscores). Use: "curriculaType". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UserRegistration" — property "curricula_permalink" uses snake_case (only DB-mirrored fields may use underscores). Use: "curriculaPermalink". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UserRegistration" — property "registration_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "registrationId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UserRegistration" — property "user_first_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "userFirstName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UserRegistration" — property "user_last_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "userLastName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UserRegistration" — property "user_email" uses snake_case (only DB-mirrored fields may use underscores). Use: "userEmail". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UserRegistration" — property "user_avatar_url" uses snake_case (only DB-mirrored fields may use underscores). Use: "userAvatarUrl". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "UserRegistration" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CurriculaRegistrationsFilter" — property "content_type" uses snake_case (only DB-mirrored fields may use underscores). Use: "contentType". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CurriculaRegistrationsResponse" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Schema "CurriculaRegistrationsResponse" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	/api/academy/admin/registrations — query parameter "content_type" uses snake_case (only DB-mirrored fields may use underscores). Use camelCase instead: "contentType". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Path "/api/academy/Curricula/registered" — segment "Curricula" must be kebab-case. Suggested: "curricula". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	Path "/api/academy/Curricula" — segment "Curricula" must be kebab-case. Suggested: "curricula". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/Curricula/registered — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/curricula — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/Curricula — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/{type}/{orgId}/{slug} — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/{type}/{orgId}/{slug} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/register — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/curricula/registrations/{id}/withdraw — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/curricula/registrations/{id}/withdraw — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/curricula/{id} — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	PUT /api/academy/curricula/{id} — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	DELETE /api/academy/curricula/{id} — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/registrations/{contentId} — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/registrations/{registrationId}/progress-tracker/update-current-item — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/registrations/{registrationId}/progress-tracker/update-current-item — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/registrations/tests — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/registrations/test-sessions/start — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/registrations/{id}/test-sessions — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/registrations/test-sessions/submit — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/admin/summary — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/admin/registrations — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/certificates/{certificateId} — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/academy/api.yml	No security schemes declared. api.yml files with path operations must define at least one entry under `components.securitySchemes` (e.g. `jwt`, `apiKey`).
schemas/constructs/v1beta1/academy/api.yml	No security requirements applied. api.yml files with operations should apply security either via a top-level `security` array (e.g. `security: [{ jwt: [] }]`) or via per-operation `security` entries. Without this, endpoints are effectively unauthenticated by default.
schemas/constructs/v1beta1/academy/api.yml	GET /api/academy/registrations/{id}/test-sessions — list endpoint (returns array or paged response) is missing pagination parameter(s): page, pagesize. Reference the shared parameters from v1alpha1/core/api.yml for consistent pagination across all list endpoints.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/registrations/{registrationId}/progress-tracker/update-current-item — response 200 has an inline schema with 5 properties. Extract it to `components/schemas` and use a `$ref` to avoid type duplication in generated clients.
schemas/constructs/v1beta1/academy/api.yml	Schema "AcademyRegistration" — property "id" has a manual `yaml:` tag in x-oapi-codegen-extra-tags. YAML struct tags are automatically added by the Go generator — remove this to avoid conflicts.
schemas/constructs/v1beta1/academy/api.yml	POST /api/academy/register — operationId "registerToAcademyContent" appears to create a resource but uses 200 instead of 201 (Created). Use 201 for POST endpoints that exclusively create new resources.
schemas/constructs/v1beta1/badge/api.yml	GET /api/organizations/badges/{id} — operationId "getBadgeByID" uses "ID" suffix instead of "Id". Use: "getBadgeById". The "Id" casing is universal across all identifiers. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/badge/api.yml	DELETE /api/organizations/badges/{id} — operationId "deleteBadgeByID" uses "ID" suffix instead of "Id". Use: "deleteBadgeById". The "Id" casing is universal across all identifiers. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/badge/api.yml	Schema "BadgePayload" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/badge/api.yml	Schema "BadgePayload" — property "image_url" uses snake_case (only DB-mirrored fields may use underscores). Use: "imageUrl". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/badge/api.yml	Schema "Badge" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/badge/api.yml	Schema "Badge" — property "image_url" uses snake_case (only DB-mirrored fields may use underscores). Use: "imageUrl". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/connection.yaml	Schema "(entity root)" — property "sub_type" uses snake_case (only DB-mirrored fields may use underscores). Use: "subType". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "ConnectionPayload" — property "sub_type" uses snake_case (only DB-mirrored fields may use underscores). Use: "subType". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "ConnectionPayload" — property "credential_secret" uses snake_case (only DB-mirrored fields may use underscores). Use: "credentialSecret". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "ConnectionsStatusPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "ConnectionsStatusPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "ConnectionsStatusPage" — property "connections_status" uses snake_case (only DB-mirrored fields may use underscores). Use: "connectionsStatus". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "MesheryInstance" — property "server_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "serverId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "MesheryInstance" — property "server_version" uses snake_case (only DB-mirrored fields may use underscores). Use: "serverVersion". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "MesheryInstance" — property "server_location" uses snake_case (only DB-mirrored fields may use underscores). Use: "serverLocation". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "MesheryInstance" — property "server_build_sha" uses snake_case (only DB-mirrored fields may use underscores). Use: "serverBuildSha". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "MesheryInstancePage" — property "meshery_instances" uses snake_case (only DB-mirrored fields may use underscores). Use: "mesheryInstances". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "MesheryInstancePage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "MesheryInstancePage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "MesheryCompatibility" — property "meshery_version" uses snake_case (only DB-mirrored fields may use underscores). Use: "mesheryVersion". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	Schema "MesheryCompatibility" — property "check_compatibility" uses snake_case (only DB-mirrored fields may use underscores). Use: "checkCompatibility". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/connection/api.yml	POST /api/environments/{environmentId}/connections/{connectionId} — operationId "addConnectionToEnvironment" appears to create a resource but uses 200 instead of 201 (Created). Use 201 for POST endpoints that exclusively create new resources.
schemas/constructs/v1beta1/credential/api.yml	GET /api/integrations/credentials/{id} — operationId "getCredentialByID" uses "ID" suffix instead of "Id". Use: "getCredentialById". The "Id" casing is universal across all identifiers. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/credential/api.yml	Schema "CredentialPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/credential/api.yml	Schema "CredentialPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/credential/api.yml	Parameter definition "credentialId" — name "credential_id" uses snake_case (only DB-mirrored fields may use underscores). Use camelCase instead: "credentialId".
schemas/constructs/v1beta1/design/api.yml	Schema "MesheryPattern" — property "catalog_data" uses snake_case (only DB-mirrored fields may use underscores). Use: "catalogData". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "MesheryPattern" — property "pattern_file" uses snake_case (only DB-mirrored fields may use underscores). Use: "patternFile". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "MesheryPatternPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "MesheryPatternPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "MesheryPatternRequestBody" — property "pattern_data" uses snake_case (only DB-mirrored fields may use underscores). Use: "patternData". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "MesheryPatternImportRequestBody" — property "file_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "fileName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "CatalogContentPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "CatalogContentPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "CatalogContentPage" — property "models_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "modelsCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "CatalogContentPage" — property "category_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "categoryCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "CatalogRequestsPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "CatalogRequestsPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "MesheryViewPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	Schema "MesheryViewPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	/api/content/patterns — query parameter "user_id" uses snake_case (only DB-mirrored fields may use underscores). Use camelCase instead: "userId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	/api/content/patterns — query parameter "orgID" uses "ID" token (must be "Id"). Use camelCase instead: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	/api/catalog/content/{type} — query parameter "orgID" uses "ID" token (must be "Id"). Use camelCase instead: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	/api/content/views — query parameter "orgID" uses "ID" token (must be "Id"). Use camelCase instead: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	/api/content/views — query parameter "user_id" uses snake_case (only DB-mirrored fields may use underscores). Use camelCase instead: "userId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/design/api.yml	POST /api/content/patterns/clone/{id} — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/design/api.yml	POST /api/content/patterns/upload/{id} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/design/api.yml	POST /api/pattern/import — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/design/api.yml	GET /api/catalog/content/{type} — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/design/api.yml	GET /api/catalog/content/{type} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/design/api.yml	POST /api/catalog/content/{type} — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/design/api.yml	POST /api/catalog/content/{type} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/design/api.yml	POST /api/catalog/content/{type}/unpublish — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/design/api.yml	POST /api/catalog/content/{type}/unpublish — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/design/api.yml	POST /api/catalog/requests/approve — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/design/api.yml	POST /api/catalog/requests/deny — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/design/api.yml	POST /api/content/filters/clone/{id} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/design/api.yml	PUT /api/content/views/{viewId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/design/api.yml	POST /api/resource/{resourceType}/share/{resourceId} — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/design/api.yml	POST /api/resource/{resourceType}/share/{resourceId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/design/api.yml	GET /api/resource/{resourceType}/share/{resourceId}/{actorType} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/design/api.yml	No security requirements applied. api.yml files with operations should apply security either via a top-level `security` array (e.g. `security: [{ jwt: [] }]`) or via per-operation `security` entries. Without this, endpoints are effectively unauthenticated by default.
schemas/constructs/v1beta1/design/api.yml	GET /api/catalog/content/classes — list endpoint (returns array or paged response) is missing pagination parameter(s): page, pagesize. Reference the shared parameters from v1alpha1/core/api.yml for consistent pagination across all list endpoints.
schemas/constructs/v1beta1/environment/api.yml	Schema "environmentPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/environment/api.yml	Schema "environmentPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/environment/api.yml	Schema "environmentConnectionsPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/environment/api.yml	Schema "environmentConnectionsPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/environment/api.yml	Schema component name "environment" must be PascalCase. Suggested: "Environment". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/environment/api.yml	Schema component name "environmentConnectionMapping" must be PascalCase. Suggested: "EnvironmentConnectionMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/environment/api.yml	Schema component name "environmentPayload" must be PascalCase. Suggested: "EnvironmentPayload". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/environment/api.yml	Schema component name "environmentPage" must be PascalCase. Suggested: "EnvironmentPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/environment/api.yml	Schema component name "environmentConnectionsPage" must be PascalCase. Suggested: "EnvironmentConnectionsPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/environment/api.yml	Parameter definition "orgIDQuery" — name "orgID" uses "ID" token (must be "Id"). Use camelCase instead: "orgId".
schemas/constructs/v1beta1/environment/api.yml	GET /api/environments/{environmentId} — list endpoint (returns array or paged response) is missing pagination parameter(s): page, pagesize. Reference the shared parameters from v1alpha1/core/api.yml for consistent pagination across all list endpoints.
schemas/constructs/v1beta1/environment/api.yml	DELETE /api/environments/{environmentId} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/event/api.yml	Schema "eventResult" — property "first_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "firstName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema "eventResult" — property "last_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "lastName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema "eventsPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema "eventsPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema "eventSummaryPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema "eventSummaryPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema component name "eventResult" must be PascalCase. Suggested: "EventResult". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema component name "eventsPage" must be PascalCase. Suggested: "EventsPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema component name "eventsAggregate" must be PascalCase. Suggested: "EventsAggregate". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema component name "eventSummary" must be PascalCase. Suggested: "EventSummary". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema component name "eventSummaryPage" must be PascalCase. Suggested: "EventSummaryPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	Schema component name "eventType" must be PascalCase. Suggested: "EventType". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/event/api.yml	DELETE /events/{id} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/feature/api.yml	Schema "Feature" — property "plan_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "planId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/feature/api.yml	Schema "FeatureName" — enum value "ComponentsInDesign" must be lowercase. Use "componentsindesign" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/feature/api.yml	Schema "FeatureName" — enum value "RelationshipsInDesign" must be lowercase. Use "relationshipsindesign" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/feature/api.yml	Schema "FeatureName" — enum value "DesignsInWorkspace" must be lowercase. Use "designsinworkspace" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/feature/api.yml	Schema "FeatureName" — enum value "WorkspacesInOrganization" must be lowercase. Use "workspacesinorganization" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/feature/api.yml	Schema "FeatureName" — enum value "ImageSizeInDesign" must be lowercase. Use "imagesizeindesign" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/feature/api.yml	Schema "FeatureName" — enum value "SizePerDesign" must be lowercase. Use "sizeperdesign" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "SignupRequestsPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "SignupRequestsPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "InvitationPayload" — property "owner_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "ownerId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "InvitationPayload" — property "is_default" uses snake_case (only DB-mirrored fields may use underscores). Use: "isDefault". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "InvitationPayload" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "InvitationPayload" — property "expires_at" uses snake_case (only DB-mirrored fields may use underscores). Use: "expiresAt". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "Invitation" — property "owner_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "ownerId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "Invitation" — property "is_default" uses snake_case (only DB-mirrored fields may use underscores). Use: "isDefault". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "Invitation" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "Invitation" — property "expires_at" uses snake_case (only DB-mirrored fields may use underscores). Use: "expiresAt". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema "Invitation" — property "accepted_by" uses snake_case (only DB-mirrored fields may use underscores). Use: "acceptedBy". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	Schema component name "uuid" must be PascalCase. Suggested: "Uuid". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/invitation/api.yml	GET /api/organizations/invitations/{invitationId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/invitation/api.yml	PUT /api/organizations/invitations/{invitationId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/invitation/api.yml	DELETE /api/organizations/invitations/{invitationId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/invitation/api.yml	POST /api/organizations/invitations/{invitationId}/accept — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/invitation/api.yml	POST /api/identity/orgs/{orgId}/users/invite — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/invitation/api.yml	POST /api/identity/orgs/{orgId}/users/invite — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/invitation/api.yml	POST /api/identity/users/request — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/invitation/api.yml	GET /api/identity/users/request/notification — missing `500` (Internal Server Error) response.
schemas/constructs/v1beta1/invitation/api.yml	No security schemes declared. api.yml files with path operations must define at least one entry under `components.securitySchemes` (e.g. `jwt`, `apiKey`).
schemas/constructs/v1beta1/invitation/api.yml	No security requirements applied. api.yml files with operations should apply security either via a top-level `security` array (e.g. `security: [{ jwt: [] }]`) or via per-operation `security` entries. Without this, endpoints are effectively unauthenticated by default.
schemas/constructs/v1beta1/key/api.yml	Schema "KeyPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/key/api.yml	Schema "KeyPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/key/api.yml	GET /api/identity/orgs/{orgId}/users/keys — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/key/api.yml	GET /api/identity/orgs/{orgId}/users/keys — list endpoint (returns array or paged response) is missing pagination parameter(s): page, pagesize. Reference the shared parameters from v1alpha1/core/api.yml for consistent pagination across all list endpoints.
schemas/constructs/v1beta1/keychain/api.yml	Schema "KeychainPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/keychain/api.yml	Schema "KeychainPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/model/api.yml	Schema "MeshModelModelsPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/model/api.yml	Schema "MeshModelModelsPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/model/api.yml	Schema "ImportRequest".uploadType — enum value "urlImport" must be lowercase. Use "urlimport" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/model/api.yml	POST /api/meshmodels/register — operationId "registerMeshmodels" appears to create a resource but uses 200 instead of 201 (Created). Use 201 for POST endpoints that exclusively create new resources.
schemas/constructs/v1beta1/organization/api.yml	Schema "Logo" — property "desktop_view" uses snake_case (only DB-mirrored fields may use underscores). Use: "desktopView". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "Logo" — property "mobile_view" uses snake_case (only DB-mirrored fields may use underscores). Use: "mobileView". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "Logo" — property "dark_desktop_view" uses snake_case (only DB-mirrored fields may use underscores). Use: "darkDesktopView". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "Logo" — property "dark_mobile_view" uses snake_case (only DB-mirrored fields may use underscores). Use: "darkMobileView". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "OrganizationsPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "OrganizationsPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "OrganizationPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "OrganizationPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "OrganizationPayload" — property "notify_org_update" uses snake_case (only DB-mirrored fields may use underscores). Use: "notifyOrgUpdate". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "AvailableTeam" — property "ID" starts with uppercase (must be camelCase, not PascalCase); uses "ID" token (must be "Id"). Use: "iD". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "TeamsPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "TeamsPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "TeamsOrganizationsMapping" — property "ID" starts with uppercase (must be camelCase, not PascalCase); uses "ID" token (must be "Id"). Use: "iD". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "TeamsOrganizationsMapping" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "TeamsOrganizationsMappingPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "TeamsOrganizationsMappingPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	Schema "TeamsOrganizationsMappingPage" — property "teams_organizations_mapping" uses snake_case (only DB-mirrored fields may use underscores). Use: "teamsOrganizationsMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/organization/api.yml	POST /api/identity/orgs — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/organization/api.yml	GET /api/identity/orgs/by-domain — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/organization/api.yml	GET /api/identity/orgs/{orgId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/organization/api.yml	PUT /api/identity/orgs/{orgId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/organization/api.yml	GET /api/identity/orgs/{orgId}/preferences — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/organization/api.yml	POST /api/identity/orgs/{orgId}/users/{userId} — missing `400` (Bad Request) response. Write operations (POST/PUT/PATCH) must define a 400 response for validation errors.
schemas/constructs/v1beta1/organization/api.yml	POST /api/identity/orgs/{orgId}/users/{userId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/organization/api.yml	DELETE /api/identity/orgs/{orgId}/users/{userId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/organization/api.yml	GET /api/identity/orgs/{orgId} — list endpoint (returns array or paged response) is missing pagination parameter(s): page, pagesize. Reference the shared parameters from v1alpha1/core/api.yml for consistent pagination across all list endpoints.
schemas/constructs/v1beta1/organization/api.yml	DELETE /api/identity/orgs/{orgId} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/organization/api.yml	POST /api/identity/orgs/{orgId}/teams/{teamId} — operationId "addTeamToOrg" appears to create a resource but uses 200 instead of 201 (Created). Use 201 for POST endpoints that exclusively create new resources.
schemas/constructs/v1beta1/organization/api.yml	POST /api/identity/orgs/{orgId}/users/{userId} — operationId "addUserToOrg" appears to create a resource but uses 200 instead of 201 (Created). Use 201 for POST endpoints that exclusively create new resources.
schemas/constructs/v1beta1/organization/api.yml	DELETE /api/identity/orgs/{orgId}/users/{userId} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/plan/api.yml	Schema "PlanPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/plan/api.yml	Schema "PlanPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/plan/api.yml	Schema "Plan" — property "minimum_units" uses snake_case (only DB-mirrored fields may use underscores). Use: "minimumUnits". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/plan/api.yml	Schema "Plan" — property "price_per_unit" uses snake_case (only DB-mirrored fields may use underscores). Use: "pricePerUnit". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/plan/api.yml	Schema "PlanName" — enum value "Free" must be lowercase. Use "free" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/plan/api.yml	Schema "PlanName" — enum value "Team Designer" must be lowercase. Use "team designer" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/plan/api.yml	Schema "PlanName" — enum value "Team Operator" must be lowercase. Use "team operator" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/plan/api.yml	Schema "PlanName" — enum value "Enterprise" must be lowercase. Use "enterprise" instead. See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "Role" — property "role_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "roleName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "RolesPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "RolesPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "RolesKeychainsMapping" — property "keychain_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "keychainId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "RolesKeychainsMapping" — property "role_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "roleId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "RoleHolderRequest" — property "role_names" uses snake_case (only DB-mirrored fields may use underscores). Use: "roleNames". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "UserRoleUpdateRequest" — property "first_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "firstName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "UserRoleUpdateRequest" — property "last_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "lastName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "UserRoleUpdateRequest" — property "role_names" uses snake_case (only DB-mirrored fields may use underscores). Use: "roleNames". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "Preference" — property "welcome_email" uses snake_case (only DB-mirrored fields may use underscores). Use: "welcomeEmail". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Schema "Preference" — property "notify_role_change" uses snake_case (only DB-mirrored fields may use underscores). Use: "notifyRoleChange". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/role/api.yml	Parameter definition "teamID" — name "teamID" uses "ID" token (must be "Id"). Use camelCase instead: "teamId".
schemas/constructs/v1beta1/role/api.yml	POST /api/identity/roles — operationId "addRoleHolder" appears to create a resource but uses 200 instead of 201 (Created). Use 201 for POST endpoints that exclusively create new resources.
schemas/constructs/v1beta1/schedule/api.yml	Schema "Schedule" — property "cron_expression" uses snake_case (only DB-mirrored fields may use underscores). Use: "cronExpression". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/schedule/api.yml	Schema "SchedulePage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/schedule/api.yml	Schema "SchedulePage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "CreateSubscriptionRequest" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "CreateSubscriptionRequest" — property "plan_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "planId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "CreateSubscriptionRequest" — property "coupon_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "couponId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "CreateSubscriptionRequest" — property "user_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "userCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "CreateSubscriptionRequest" — property "payment_processor" uses snake_case (only DB-mirrored fields may use underscores). Use: "paymentProcessor". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "UpgradeSubscriptionRequest" — property "old_plan_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "oldPlanId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "UpgradeSubscriptionRequest" — property "new_plan_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "newPlanId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "CreateSubscriptionResponse" — property "subscription_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "subscriptionId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "UpdateUsersRequest" — property "payment_processor" uses snake_case (only DB-mirrored fields may use underscores). Use: "paymentProcessor". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "CancelSubscriptionRequest" — property "subscription_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "subscriptionId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "CancelSubscriptionRequest" — property "payment_processor" uses snake_case (only DB-mirrored fields may use underscores). Use: "paymentProcessor". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "SubscriptionPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "SubscriptionPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "Subscription" — property "ID" starts with uppercase (must be camelCase, not PascalCase); uses "ID" token (must be "Id"). Use: "iD". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "Subscription" — property "org_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "orgId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "Subscription" — property "plan_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "planId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "Subscription" — property "start_date" uses snake_case (only DB-mirrored fields may use underscores). Use: "startDate". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "Subscription" — property "end_date" uses snake_case (only DB-mirrored fields may use underscores). Use: "endDate". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Schema "Subscription" — property "billing_id" uses snake_case (only DB-mirrored fields may use underscores). Use: "billingId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/subscription/api.yml	Path "/api/entitlement/subscriptions/{subscriptionId}/upgradePreview" — segment "upgradePreview" must be kebab-case. Suggested: "upgrade-preview". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema "teamPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema "teamPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema "teamsUsersMappingPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema "teamsUsersMappingPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema "teamsUsersMappingPage" — property "teams_users_mapping" uses snake_case (only DB-mirrored fields may use underscores). Use: "teamsUsersMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema "teamMembersPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema "teamMembersPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema component name "team" must be PascalCase. Suggested: "Team". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema component name "teamPayload" must be PascalCase. Suggested: "TeamPayload". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema component name "teamUpdatePayload" must be PascalCase. Suggested: "TeamUpdatePayload". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema component name "teamPage" must be PascalCase. Suggested: "TeamPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema component name "teamsUsersMapping" must be PascalCase. Suggested: "TeamsUsersMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema component name "teamsUsersMappingPage" must be PascalCase. Suggested: "TeamsUsersMappingPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema component name "teamMember" must be PascalCase. Suggested: "TeamMember". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	Schema component name "teamMembersPage" must be PascalCase. Suggested: "TeamMembersPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/team/api.yml	GET /api/identity/orgs/{orgId}/teams — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/team/api.yml	POST /api/identity/orgs/{orgId}/teams — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/team/api.yml	POST /api/identity/orgs/{orgId}/teams — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/team/api.yml	GET /api/identity/teams/{teamId}/users — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/team/api.yml	POST /api/identity/orgs/{orgId}/teams/{teamId}/users/{userId} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/team/api.yml	GET /api/identity/orgs/{orgId}/teams/{teamId}/users — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/team/api.yml	POST /api/identity/orgs/{orgId}/teams/{teamId}/users/{userId} — operationId "addUserToTeam" appears to create a resource but uses 200 instead of 201 (Created). Use 201 for POST endpoints that exclusively create new resources.
schemas/constructs/v1beta1/team/api.yml	DELETE /api/identity/orgs/{orgId}/teams/{teamId}/users/{userId} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/token/api.yml	Schema "UserToken" — property "access_token" uses snake_case (only DB-mirrored fields may use underscores). Use: "accessToken". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/token/api.yml	Schema "UserToken" — property "refresh_token" uses snake_case (only DB-mirrored fields may use underscores). Use: "refreshToken". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/token/api.yml	Schema "UserToken" — property "is_oauth" uses snake_case (only DB-mirrored fields may use underscores). Use: "isOauth". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/token/api.yml	Schema "TokenPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/token/api.yml	Schema "TokenPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/token/api.yml	Parameter definition "tokenId" — name "token_id" uses snake_case (only DB-mirrored fields may use underscores). Use camelCase instead: "tokenId".
schemas/constructs/v1beta1/token/api.yml	Parameter definition "userId" — name "user_id" uses snake_case (only DB-mirrored fields may use underscores). Use camelCase instead: "userId".
schemas/constructs/v1beta1/token/api.yml	GET /api/identity/tokens/infinite — list endpoint (returns array or paged response) is missing pagination parameter(s): page, pagesize. Reference the shared parameters from v1alpha1/core/api.yml for consistent pagination across all list endpoints.
schemas/constructs/v1beta1/user/api.yml	Schema "User" — property "first_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "firstName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Schema "User" — property "last_name" uses snake_case (only DB-mirrored fields may use underscores). Use: "lastName". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Schema "User" — property "first_login_time" uses snake_case (only DB-mirrored fields may use underscores). Use: "firstLoginTime". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Schema "User" — property "last_login_time" uses snake_case (only DB-mirrored fields may use underscores). Use: "lastLoginTime". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Schema "User" — property "role_names" uses snake_case (only DB-mirrored fields may use underscores). Use: "roleNames". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Schema "UsersPageForAdmin" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Schema "UsersPageForAdmin" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Schema "UsersPageForNonAdmin" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Schema "UsersPageForNonAdmin" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Schema "Preference" — property "selectedOrganizationID" uses "ID" token (must be "Id"). Use: "selectedOrganizationId". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/user/api.yml	Parameter definition "teamID" — name "teamID" uses "ID" token (must be "Id"). Use camelCase instead: "teamId".
schemas/constructs/v1beta1/user/api.yml	GET /api/identity/orgs/{orgId}/users — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/user/api.yml	GET /api/identity/users/profile/{id} — missing `401` (Unauthorized) response. All operations must define standard error responses for typed error handling in RTK clients and Go server stubs.
schemas/constructs/v1beta1/user/api.yml	GET /api/identity/users/profile/{id} — missing `404` (Not Found) response. Operations on parameterized paths must define a 404 response.
schemas/constructs/v1beta1/user/api.yml	No security requirements applied. api.yml files with operations should apply security either via a top-level `security` array (e.g. `security: [{ jwt: [] }]`) or via per-operation `security` entries. Without this, endpoints are effectively unauthenticated by default.
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspace" — property "ID" starts with uppercase (must be camelCase, not PascalCase); uses "ID" token (must be "Id"). Use: "iD". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesTeamsMapping" — property "ID" starts with uppercase (must be camelCase, not PascalCase); uses "ID" token (must be "Id"). Use: "iD". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesEnvironmentsMapping" — property "ID" starts with uppercase (must be camelCase, not PascalCase); uses "ID" token (must be "Id"). Use: "iD". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesViewsMapping" — property "ID" starts with uppercase (must be camelCase, not PascalCase); uses "ID" token (must be "Id"). Use: "iD". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesDesignsMapping" — property "ID" starts with uppercase (must be camelCase, not PascalCase); uses "ID" token (must be "Id"). Use: "iD". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacePage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacePage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesTeamsMappingPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesTeamsMappingPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesTeamsMappingPage" — property "workspaces_teams_mapping" uses snake_case (only DB-mirrored fields may use underscores). Use: "workspacesTeamsMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesEnvironmentsMappingPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesEnvironmentsMappingPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesEnvironmentsMappingPage" — property "workspaces_environments_mapping" uses snake_case (only DB-mirrored fields may use underscores). Use: "workspacesEnvironmentsMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesDesignsMappingPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesDesignsMappingPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesDesignsMappingPage" — property "workspaces_designs_mapping" uses snake_case (only DB-mirrored fields may use underscores). Use: "workspacesDesignsMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesViewsMappingPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesViewsMappingPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "workspacesViewsMappingPage" — property "workspaces_views_mapping" uses snake_case (only DB-mirrored fields may use underscores). Use: "workspacesViewsMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "mesheryDesignPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "mesheryDesignPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "mesheryView" — property "ID" starts with uppercase (must be camelCase, not PascalCase); uses "ID" token (must be "Id"). Use: "iD". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "mesheryViewPage" — property "page_size" uses snake_case (only DB-mirrored fields may use underscores). Use: "pageSize". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema "mesheryViewPage" — property "total_count" uses snake_case (only DB-mirrored fields may use underscores). Use: "totalCount". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspace" must be PascalCase. Suggested: "Workspace". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacesTeamsMapping" must be PascalCase. Suggested: "WorkspacesTeamsMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacesEnvironmentsMapping" must be PascalCase. Suggested: "WorkspacesEnvironmentsMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacesViewsMapping" must be PascalCase. Suggested: "WorkspacesViewsMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacesDesignsMapping" must be PascalCase. Suggested: "WorkspacesDesignsMapping". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacePayload" must be PascalCase. Suggested: "WorkspacePayload". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspaceUpdatePayload" must be PascalCase. Suggested: "WorkspaceUpdatePayload". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacePage" must be PascalCase. Suggested: "WorkspacePage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacesTeamsMappingPage" must be PascalCase. Suggested: "WorkspacesTeamsMappingPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacesEnvironmentsMappingPage" must be PascalCase. Suggested: "WorkspacesEnvironmentsMappingPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacesDesignsMappingPage" must be PascalCase. Suggested: "WorkspacesDesignsMappingPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "workspacesViewsMappingPage" must be PascalCase. Suggested: "WorkspacesViewsMappingPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "mesheryDesignPage" must be PascalCase. Suggested: "MesheryDesignPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "mesheryView" must be PascalCase. Suggested: "MesheryView". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	Schema component name "mesheryViewPage" must be PascalCase. Suggested: "MesheryViewPage". See AGENTS.md § "Casing rules at a glance".
schemas/constructs/v1beta1/workspace/api.yml	DELETE /api/workspaces/{workspaceId} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/workspace/api.yml	DELETE /api/workspaces/{workspaceId}/teams/{teamId} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/workspace/api.yml	DELETE /api/workspaces/{workspaceId}/environments/{environmentId} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/workspace/api.yml	DELETE /api/workspaces/{workspaceId}/designs/{designId} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/workspace/api.yml	DELETE /api/workspaces/{workspaceId}/views/{viewId} — single-resource DELETE should return 204 (No Content) instead of 200. Use 204 when the response body is empty after deletion.
schemas/constructs/v1beta1/academy/api.yml	Duplicate schema structure detected across constructs: CurriculaRegistrationsResponse (schemas/constructs/v1beta1/academy/api.yml), eventSummaryPage (schemas/constructs/v1beta1/event/api.yml), SignupRequestsPage (schemas/constructs/v1beta1/invitation/api.yml), teamMembersPage (schemas/constructs/v1beta1/team/api.yml), UsersPageForAdmin (schemas/constructs/v1beta1/user/api.yml), UsersPageForNonAdmin (schemas/constructs/v1beta1/user/api.yml). Consider using a cross-construct `$ref` to a single canonical definition to avoid type drift.
